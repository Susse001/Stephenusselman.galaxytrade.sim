spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gts
    username: gts
    password: gtspassword

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true