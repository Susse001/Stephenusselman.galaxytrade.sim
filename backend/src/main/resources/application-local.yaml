spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gts
    username: gts
    password: gtspassword

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

    properties:
      hibernate:
        format_sql: true