spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gts-postgres
    username: gts
    password: gts

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

    properties:
      hibernate:
        format_sql: true